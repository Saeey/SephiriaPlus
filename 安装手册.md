# SephiriaPlus 2.2.0 安装手册

## 一、准备 BepInEx

SephiriaPlus 2.0.0 需要 64 位 BepInEx 5。

1. 下载 `BepInEx_win_x64_5.4.23.5.zip`。
2. 在 Steam 中右键《Sephiria》→“管理”→“浏览本地文件”。
3. 把压缩包内所有文件解压到 `Sephiria.exe` 所在目录。
4. 启动游戏一次，进入主菜单后退出。

成功后，游戏目录中会出现 `BepInEx` 文件夹和 `BepInEx/LogOutput.log`。

## 二、安装 SephiriaPlus

1. 下载 `SephiriaPlus-v2.0.0.zip` 并解压。
2. 将压缩包中的 `BepInEx` 文件夹拖入游戏根目录，选择合并。
3. 检查以下文件是否存在：

```text
Sephiria/
└─ BepInEx/
   └─ plugins/
      └─ SephiriaPlus/
         ├─ SephiriaPlus.dll
         └─ config.json
```

如果曾安装 1.x AddOn 版，请先删除：

```text
Sephiria/AddOns/SephiriaPlus
```

## 三、确认是否加载成功

启动游戏后打开：

```text
Sephiria/BepInEx/LogOutput.log
```

能看到 `Loading [SephiriaPlus 2.2.0]` 即代表插件已加载。

## 四、功能与按键

- F8：失败界面重试当前关卡。
- 无限刷新、天赋 ×10、背包 +18、隐藏房间提示、许愿池容量 +50 默认开启。
- 神器流派筛选正在重新适配，不会出现在奇迹三选一界面。

所有功能都能在 `BepInEx/plugins/SephiriaPlus/config.json` 中单独开关。修改配置后需要重启游戏。

## 五、常见问题

### 游戏启动了但 Mod 没效果

先确认没有把文件多套了一层目录。正确路径是 `BepInEx/plugins/SephiriaPlus/SephiriaPlus.dll`，不是 `SephiriaPlus-v2.0.0/BepInEx/...`。

### 没有生成 LogOutput.log

BepInEx 本体没有正确放到游戏根目录，或者下载了错误架构。请使用 Windows x64 的 BepInEx 5 包。

### 联机队友能否看到效果

房主权威的逻辑修改通常由房主生效。为了减少版本差异，联机玩家最好安装相同版本。

### 如何卸载

退出游戏后删除：

```text
Sephiria/BepInEx/plugins/SephiriaPlus
```

BepInEx 可以保留给其他 Mod 使用。
