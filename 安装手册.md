# 《赛菲莉娅》房主增强 Mod 安装手册

适用 Mod：`SephiriaPlus`  
当前版本：`v1.4.0`  
适用平台：Steam Windows 版《Sephiria / 赛菲莉娅》

## 一、功能说明

房主安装后，本房间内所有玩家获得以下效果：

- 刷新骰子低于 99 时自动补充至 99。
- 命运刻印提供的天赋点变为原版的 10 倍；初始基础天赋点不参与倍率。
- 主背包增加 18 格，即增加 3 整行，默认背包由 24 格扩展至 42 格。

联机时只需房主安装。队友无需安装；普通队员单独安装不会生效。

## 二、安装前准备

1. 完全关闭游戏。
2. 建议备份存档，特别是准备在已有进度或联机存档中使用时。
3. 本 Mod 使用游戏自带的 AddOn 加载器，不需要安装 MelonLoader 或 BepInEx。

存档通常位于：

```text
C:\Users\你的用户名\AppData\LocalLow\TEAMHORAY\Sephiria
```

可以按 `Win + R`，输入下面的内容后回车快速打开：

```text
%USERPROFILE%\AppData\LocalLow\TEAMHORAY\Sephiria
```

将整个 `Sephiria` 文件夹复制到安全位置即可完成备份。

## 三、找到游戏目录

1. 打开 Steam。
2. 在游戏库中右键《赛菲莉娅》。
3. 选择“管理” → “浏览本地文件”。
4. 确认打开的目录中可以看到 `Sephiria.exe`。

常见安装路径示例：

```text
C:\Program Files (x86)\Steam\steamapps\common\Sephiria
```

## 四、安装步骤

1. 解压下载的 Mod 压缩包。
2. 将压缩包中的 `SephiriaPlus` 文件夹复制到游戏根目录下的 `AddOns` 文件夹。
3. 如果游戏根目录中没有 `AddOns`，请手动新建该文件夹。

安装完成后，目录结构必须是：

```text
Sephiria\
├─ Sephiria.exe
└─ AddOns\
   └─ SephiriaPlus\
      ├─ SephiriaPlus.dll
      ├─ metadata.json
      └─ config.json
```

请注意：

- 不要只复制 DLL。
- 不要把压缩包直接放进 `AddOns`。
- 不要形成 `SephiriaPlus\SephiriaPlus\SephiriaPlus.dll` 这样的双层目录。
- 不要把文件放进 `Sephiria_Data`、`Managed`、`Mods` 或 `BepInEx` 文件夹。

## 五、联机使用方法

1. 由安装了 Mod 的玩家创建联机房间并担任房主。
2. 其他玩家正常加入房间，无需安装 Mod。
3. 进入城镇或地牢后，Mod 会由房主服务器统一应用效果。
4. 如果房主迁移、退出或改由未安装 Mod 的玩家重新开房，效果将不再生效。

每次进入新关卡后，房主端会自动记录入口检查点。挑战失败并出现结算界面后，房主按 `F8` 即可让全队恢复到进入该关卡时的状态并重新挑战；检查点不会因为失败而消耗，可以无限重试。队伍成员发生变化时请重新进入一个新关卡生成检查点。

请提前征得所有联机玩家同意，不要在不知情的公开房间中使用修改效果。

## 六、确认是否加载成功

启动游戏并进入城镇或地牢后，打开：

```text
%USERPROFILE%\AppData\LocalLow\TEAMHORAY\Sephiria\Player.log
```

搜索 `SephiriaPlus`，正常情况下可以看到类似内容：

```text
[AddOnLoader] 'SephiriaPlus' v1.4.0
[SephiriaPlus] loaded with config: reroll=True (target 99), talent=True (x10), inventory=True (+18), checkpointRetry=True (F8)
```

### 功能开关

关闭游戏后，用记事本打开 `Sephiria\AddOns\SephiriaPlus\config.json`：

- `EnableInfiniteReroll`：无限刷新骰子。
- `EnableTalentPointMultiplier`：命运刻印天赋点倍率。
- `EnableExtraInventorySlots`：额外背包格。
- `EnableCheckpointRetry`：失败后恢复关卡入口检查点。

将 `true` 改成 `false` 即可关闭对应功能。`RerollDiceTarget`、`TalentPointMultiplier` 和 `ExtraInventorySlots` 分别控制目标骰子数、天赋倍率和额外格数；`CheckpointRetryKey` 设置房主在失败结算界面使用的重试按键。保存后需要重新启动游戏。

进入游戏后还可以通过以下现象确认：

- 消耗刷新骰子后，数量会在约 0.25 秒内补至 99。
- 打开主背包后，比原版多出 3 行。
- 天赋界面中，命运刻印提供的额外天赋点变为原版的 10 倍。

例如原本为“基础 5 点＋刻印 10 点”，安装后应为“基础 5 点＋刻印 100 点”，合计 105 点。只有基础 5 点、尚未取得额外刻印点数时，界面仍显示 5 点是正常现象。

## 七、常见问题

### 1. 游戏中完全没有效果

- 确认当前开房者安装了 Mod；队员安装、房主未安装时不会生效。
- 确认两个文件没有放错目录。
- 确认安装或更新 Mod 时游戏已完全关闭。
- 查看 `Player.log` 中是否存在 `SephiriaPlus` 或红色异常信息。

### 2. 骰子有效，但天赋点仍是 5

基础 5 点不会乘 10。只有通过命运刻印获得的额外天赋点参与倍率；先确认存档已经取得相关命运刻印奖励。

### 3. 背包没有增加 18 格

关闭背包后重新打开；如果仍未更新，请让房主重新建立房间。队员单独安装无法扩容。

### 4. 更新 Mod 时提示文件正在使用

说明游戏尚未完全退出。关闭游戏并在任务管理器中确认没有 `Sephiria.exe` 后再覆盖文件。

### 5. 游戏更新后 Mod 失效或报错

《赛菲莉娅》更新可能改变内部接口。先移除 Mod 验证原版能否正常运行，再等待与新版本兼容的 Mod 更新。

## 八、卸载与存档注意事项

1. 完全关闭游戏。
2. 删除以下整个文件夹：

```text
Sephiria\AddOns\SephiriaPlus
```

3. 重新启动游戏。

卸载前建议：

- 将额外 18 格中的物品移动到原版背包范围，避免继续游玩该局时出现物品位置异常。
- 重置天赋并将已分配点数控制在原版上限以内。
- 最稳妥的做法是结束当前局后再卸载。

如果卸载后出现异常，请恢复安装前的存档备份，或通过 Steam 的“验证游戏文件完整性”恢复原版文件。验证文件不会自动删除 `AddOns` 中的第三方 Mod，因此仍需手动删除 Mod 文件夹。

## 九、免责声明

本 Mod 是非官方玩家作品，与 TEAM HORAY、Steam 无关联。游戏更新可能导致 Mod 失效。使用者应自行备份存档并承担使用风险。本 Mod 会改变游戏平衡，请勿在其他玩家不知情的情况下用于联机游戏。
