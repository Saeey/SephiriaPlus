# SephiriaPlus 2.2.4 安装手册

## 一、一体化安装

SephiriaPlus 2.2.4 发布包已内置 Windows x64 版 BepInEx 5.4.23.5，不需要再单独下载 BepInEx。

1. 下载 `SephiriaPlus-v2.2.4.zip`。
2. 在 Steam 中右键《Sephiria》→“管理”→“浏览本地文件”。
3. 把发布包内的全部文件直接解压到 `Sephiria.exe` 所在目录，已有文件选择覆盖或合并。
4. 启动游戏。首次启动时 BepInEx 会自动生成配置和日志文件，时间可能比平时稍长。
5. 检查以下文件是否存在：

```text
Sephiria/
├─ Sephiria.exe
├─ winhttp.dll
├─ doorstop_config.ini
├─ .doorstop_version
└─ BepInEx/
   ├─ core/
   └─ plugins/
      └─ SephiriaPlus/
         ├─ SephiriaPlus.dll
         └─ config.json
```

其中 `winhttp.dll` 负责加载 Unity Doorstop，`doorstop_config.ini` 指向 BepInEx 预加载器，`BepInEx/core` 存放 BepInEx 核心组件；三部分必须配套存在。`.doorstop_version` 和 `changelog.txt` 来自官方发布包，也一并保留。

如果曾安装 1.x AddOn 版，请先删除：

```text
Sephiria/AddOns/SephiriaPlus
```

## 二、确认是否加载成功

启动游戏后打开：

```text
Sephiria/BepInEx/LogOutput.log
```

能看到 `Loading [SephiriaPlus 2.2.4]` 即代表插件已加载。

## 三、功能与按键

- F8：失败界面重试当前关卡。
- 无限刷新、天赋 ×10、背包 +30、隐藏房间提示（放大 5 倍）、许愿池容量 +100 默认开启。
- 神器流派筛选正在重新适配，不会出现在奇迹三选一界面。

所有功能都能在 `BepInEx/plugins/SephiriaPlus/config.json` 中单独开关。修改配置后需要重启游戏。

## 四、常见问题

### 游戏启动了但 Mod 没效果

先确认没有把文件多套了一层目录。正确路径是 `BepInEx/plugins/SephiriaPlus/SephiriaPlus.dll`，不是 `SephiriaPlus-v2.2.4/BepInEx/...`。

### 没有生成 LogOutput.log

BepInEx 启动文件没有正确放到游戏根目录。确认 `winhttp.dll`、`doorstop_config.ini` 与 `Sephiria.exe` 在同一层，并确认 `BepInEx/core/BepInEx.Preloader.dll` 存在。

### 联机队友能否看到效果

房主权威的逻辑修改通常由房主生效。为了减少版本差异，联机玩家最好安装相同版本。

### 如何卸载

退出游戏后删除：

```text
Sephiria/BepInEx/plugins/SephiriaPlus
```

BepInEx 可以保留给其他 Mod 使用。
